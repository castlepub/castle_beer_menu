[build]
builder = "nixpacks"

[deploy]
releaseCommand = "npx prisma generate && npx prisma db push --force-reset && node scripts/seed.js"
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[env]
NODE_ENV = "production"
PORT = "3000" 